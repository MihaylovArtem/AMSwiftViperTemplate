//
//  {{ module_info.file_name }}
//  {{ module_info.project_name }}, {{ module_info.name }}
//
//  Created by {{ developer.name }} on {{ date }}.
//  Copyright © {{ year }} {{ developer.company }}. All rights reserved.
//

import UIKit

class {{ prefix }}{{ module_info.name }}Wireframe: NSObject {

    static let shared = {{ prefix }}{{ module_info.name }}Wireframe()
    private override init() { super.init() }

    lazy var presenter = {{ prefix }}{{ module_info.name }}Presenter()
    lazy var router = {{ prefix }}{{ module_info.name }}Router()
    lazy var interactor = {{ prefix }}{{ module_info.name }}Interactor()
    lazy var view = {{ prefix }}{{ module_info.name }}ViewController(nibName: "<#XIB name#>", bundle: nil)

    static public func initModule() {

        shared.presenter.view = shared.view
        shared.presenter.router = shared.router
        shared.presenter.interactor = shared.interactor

        shared.view.presenter = shared.presenter

        shared.view.presenter.viewDidConfigure()

    }

    static public var viewController: UIViewController {
        return shared.view
    }

}
