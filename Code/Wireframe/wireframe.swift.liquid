//
//  {{ module_info.file_name }}
//  {{ module_info.project_name }}, {{ module_info.name }}
//
//  Created by {{ developer.name }} on {{ date }}.
//  Copyright © {{ year }} {{ developer.company }}. All rights reserved.
//

import UIKit

class {{ prefix }}{{ module_info.name }}Wireframe: NSObject {

    weak var {{ module_info.name | downcase }}ViewController: {{ prefix }}{{ module_info.name }}ViewController?

    func initModule() {
        {{ module_info.name | downcase }}ViewController = UIStoryboard(name: "<#Storyboard name#>", bundle: nil).instantiateViewController(withIdentifier: "<#VC identifier#>") as? {{ prefix }}{{ module_info.name }}ViewController
        configureModuleForViewInput(viewInput: {{ module_info.name | downcase }}ViewController)
    }

    func configureModuleForViewInput<UIViewController>(viewInput: UIViewController) {
        if let viewController = viewInput as? {{ prefix }}{{ module_info.name }}ViewController {
            configure(viewController: viewController)
        }
    }

    private func configure(viewController: {{ prefix }}{{ module_info.name }}ViewController) {
        let router = {{ prefix }}{{ module_info.name }}Router()

        let presenter = {{ prefix }}{{ module_info.name }}Presenter()
        presenter.view = viewController
        presenter.router = router

        let interactor = {{ prefix }}{{ module_info.name }}Interactor()
        interactor.output = presenter

        presenter.interactor = interactor
        viewController.output = presenter
    }


}
