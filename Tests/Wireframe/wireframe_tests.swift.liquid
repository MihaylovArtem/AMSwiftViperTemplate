//
//  {{ module_info.file_name }}
//  {{ module_info.project_name }}, {{ module_info.name }}
//
//  Created by {{ developer.name }} on {{ date }}.
//  Copyright © {{ year }} {{ developer.company }}. All rights reserved.
//

import XCTest
@testable import {{ module_info.project_name }}

class {{ prefix }}{{ module_info.name }}ModuleWireframeTests: XCTestCase {

    override func setUp() {
        super.setUp()
    }

    override func tearDown() {
        super.tearDown()
    }

    func testInitModule() {
        {{ prefix }}{{ module_info.name }}Wireframe.initModule()

        XCTAssertNotNil({{ prefix }}{{ module_info.name }}Wireframe.view.output, "output in {{ prefix }}{{ module_info.name }}ViewController is nil after configuration")
        XCTAssertTrue({{ prefix }}{{ module_info.name }}Wireframe.view.output is {{ prefix }}{{ module_info.name }}Presenter, "output is not {{ prefix }}{{ module_info.name }}Presenter")

        XCTAssertNotNil({{ prefix }}{{ module_info.name }}Wireframe.presenter.view, "view in {{ prefix }}{{ module_info.name }}Presenter is nil after configuration")
        XCTAssertNotNil({{ prefix }}{{ module_info.name }}Wireframe.presenter.router, "router in {{ prefix }}{{ module_info.name }}Presenter is nil after configuration")
        XCTAssertNotNil({{ prefix }}{{ module_info.name }}Wireframe.presenter.interactor, "interactor in {{ prefix }}{{ module_info.name }}Presenter is nil after configuration")

        XCTAssertTrue({{ prefix }}{{ module_info.name }}Wireframe.presenter.view is {{ prefix }}{{ module_info.name }}ViewController, "router is not {{ prefix }}{{ module_info.name }}ViewController")
        XCTAssertTrue({{ prefix }}{{ module_info.name }}Wireframe.presenter.router is {{ prefix }}{{ module_info.name }}Router, "router is not {{ prefix }}{{ module_info.name }}Router")
        XCTAssertTrue({{ prefix }}{{ module_info.name }}Wireframe.presenter.interactor is {{ prefix }}{{ module_info.name }}Interactor, "router is not {{ prefix }}{{ module_info.name }}Interactor")
    }

}
